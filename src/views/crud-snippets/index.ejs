<div id="snippets">
  <!-- <h1>SnippetRoom</h1>
  <h2>Code sharing made simple</h2>  -->
  <% if (viewData?.crudSnippets?.length) { %>
    <% for (const snippet of viewData.crudSnippets) { %>
      <div class="snippet">
        <div class="title">
          <h3><%= snippet.title %></h3>
        </div>
        <span class="author"><%=snippet.language%> | <%=snippet.createdAt%> | By <%=snippet.user%>
          <% if (snippet.creator() === true) { %>
            | <a href="./<%= snippet.id %>/update">Update</a>
            | <a href="./<%= snippet.id %>/delete">Delete</a>
          <% } %>
        </span>
        <div class="tags">
          <% if (snippet.tags?.length !== 0) { %>
            <% for (const tag of snippet.tags) { %>
              <a href="./<%= tag %>"><%=tag%></a>
              <% } %>
           <% } %>
        </div>
        <div class="description">
          <p><%= snippet.description %></p>
        </div>
        <div class="code">
          <pre><code><%= snippet.snippet %></code></pre>
        </div>
      </div>
    <% } %>
  <% } else { %>
    <p>No snippets to be displayed...</p>
  <% } %>
</div>